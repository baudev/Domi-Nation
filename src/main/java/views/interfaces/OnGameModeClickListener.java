package views.interfaces;

import models.enums.GameMode;

/**
 * When a {@link GameMode} button is clicked.
 */
public interface OnGameModeClickListener {

    void onGameModeClickListener(GameMode gameMode);

}
