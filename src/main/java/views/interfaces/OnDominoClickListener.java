package views.interfaces;

import models.classes.Domino;

/**
 * When the {@link models.classes.Player} click on a {@link Domino}.
 */
public interface OnDominoClickListener {

    void onDominoClickListener(Domino domino);

}
